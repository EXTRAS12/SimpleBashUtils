#include <stdbool.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#ifndef GLOBAL_H
#define GLOBAL_H

typedef struct flags {
  bool b, e, n, s, t, v, i, c, l, f, h, o;
} flags;

#endif